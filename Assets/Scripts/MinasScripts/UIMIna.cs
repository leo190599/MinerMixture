using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UIMIna : MonoBehaviour
{
    // Start is called before the first frame update
    public void destruir()
    {
        Destroy(gameObject);
    }
}
